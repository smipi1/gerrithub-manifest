#!/bin/sh /etc/rc.common

START=62
STOP=62
USE_PROCD=1

start_service() {
        mkdir -p /var/lib

        procd_open_instance
        procd_set_param command avahi-autoipd -s -w br-lan
        procd_set_param respawn
        procd_close_instance
}